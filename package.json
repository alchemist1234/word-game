{
  "name": "word-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "doc"
  },
  "scripts": {
    "test": "npm --prefix server test && npm --prefix client test",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "verify": "npm run typecheck && npm test && npm --prefix server run build && npm --prefix client run build:h5 && npm --prefix client run build:mp-weixin"
  },
  "keywords": [],
  "type": "commonjs",
  "dependencies": {
    "puppeteer-core": "^21.11.0",
    "ws": "^8.21.2"
  }
}
