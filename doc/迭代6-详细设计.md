# 迭代 6 详细设计：实时 1v1 对战

> 配套：《迭代计划.md》迭代 6、《游戏设计文档.md》(GDD v0.2) §4.2.2 §6.1 §7.4.3 §7.7、《迭代5-详细设计.md》§3（ws 网关基础）
> 阶段：详细设计（交付给 AI coding 执行）
> 前置：迭代 5 已完成（划词判定优化 WebSocket + Boss 关 + 6×6 + 第 4-6 章 + 词库 1.5 万）

---

## 1. 概述

### 1.1 目标
实现真人 WebSocket 实时 1v1 对战：匹配成功 → 双方同网格同时间对战 3 分钟 → 分数实时广播 → 结算判胜负。这是技术难度最高的迭代。

### 1.2 可运行状态
大厅 → 实时对战 → 匹配动画；两个 H5 标签页匹配成功，同网格开局（3-2-1 倒计时），3 分钟对战双方分数条实时同步；时间到结算页胜负对比（含稀有词数/连击峰值 tie-break）；断线 30s 内重连恢复；匹配 30s 超时提示未匹配。

### 1.3 范围
- **In**：大厅首页（GDD §6.1 P0）、WS 房间模型、Redis 匹配队列、同网格下发、实时计分广播（增量）、服务端权威计时、断线重连（30s）、matches/match_players 落库、前端对战页 + 对战结算对比、后端单测
- **Out**：AI 兜底（技术债 #9 → 迭代 8）、4 人混战/段位赛（→ 8）、好友挑战/每日挑战/排行榜（→ 7）、对战过程可见对方词（GDD §4.2.2 防抄：仅分数条）

### 1.4 对齐 GDD
- §4.2.2 实时对战（匹配/同网格/防抄/得分广播/结算 tie-break）
- §6.1 信息架构（大厅首页 P0）
- §7.4.3 matches 对局表
- §7.7 实时对战架构（匹配 → 开局 → 对局 → 结束；断线 30s）

---

## 2. 总体架构

```
玩家 A ─┐                     ┌─ 玩家 B
        ├─ WebSocket 房间 ────┤
        └─ GameGateway ───────┘
              │ userIdToClient Map
        MatchService（房间 Map + 计时 + 胜负 + 落库）
              │
          Redis（match_session:A / match_session:B + match:{id} 元数据）
              │
          PostgreSQL（matches + match_players）
```

### 2.1 核心决策：每玩家独立 `match_session`（最大复用）

**对战不新建计分通道，复用迭代 5 的 `match_session` 全链路**：

- 配对成功时，为每个玩家各建一个 `match_session:{sid}`（复用 `GameService.getGrid` 逻辑），会话 hash 增加 `matchId` 字段
- 客户端提词仍发 `submit_word`（带自己的 sid）→ `GameService.submitWord` 原逻辑不动（路径/拼字/潜在词池/连击/计分）
- 结算复用 `GameService.endGame`（已含图鉴批量收集 + unfoundWords 计算）
- **perfect 提前结束仅限单人**：`submitWord` 检测到 `session.matchId` 存在时跳过 perfect 分支（对战固定 3 分钟，不提前结束）

### 2.2 模块依赖（循环依赖处理）

`GameGateway`（GameModule）需要 `MatchService` 广播/断线通知；`MatchService`（MatchModule）需要 `GameService.getGrid` 建会话 + `GameGateway` 广播。**双向 `forwardRef`**：

```typescript
// game.module.ts
imports: [ ..., forwardRef(() => MatchModule) ]
exports: [GameService, GameGateway]

// match.module.ts
imports: [
  forwardRef(() => GameModule), GridPoolModule, DictionaryModule, AuthModule,
  TypeOrmModule.forFeature([MatchEntity, MatchPlayerEntity]),
]
controllers: [MatchController]
providers: [MatchService]
exports: [MatchService]
```

### 2.3 连接与广播

- `GameGateway` 新增 `userIdToClient = new Map<number, WebSocket>()`（`handleConnection` 注册、`handleDisconnect` 删除；clientData WeakMap 保留）
- `MatchService` 持有 `rooms: Map<matchId, MatchRoom>`；所有广播经 `GameGateway.broadcastToUser(userId, event, data)` 发送（ws 原生 `client.send`，JSON 帧）

---

## 3. 数据模型（PostgreSQL）

### 3.1 `matches`（对齐 GDD §7.4.3）

```typescript
@Entity('matches')
export class MatchEntity {
  @PrimaryColumn('uuid') id!: string
  @Column({ type: 'varchar', length: 16, default: 'pvp_1v1' }) type!: string
  @Column({ type: 'varchar', length: 64 }) gridSeed!: string      // grid_pool.id（gridUuid）
  @Column({ type: 'jsonb' }) grid!: string[][]                    // 双方同网格
  @Column({ type: 'jsonb' }) targetWords!: string[]
  @Column({ type: 'varchar', length: 16, default: 'ongoing' }) status!: string // ongoing/finished
  @Column({ type: 'bigint', nullable: true }) winnerId!: number | null
  @Column({ type: 'timestamptz' }) startedAt!: Date
  @Column({ type: 'timestamptz', nullable: true }) endedAt!: Date | null
}
```

### 3.2 `match_players`（GDD 注释：对局玩家分数落库）

```typescript
@Entity('match_players')
export class MatchPlayerEntity {
  @PrimaryGeneratedColumn() id!: number
  @Column('uuid') matchId!: string
  @Column({ type: 'bigint' }) userId!: number
  @Column({ type: 'int', default: 0 }) score!: number
  @Column({ type: 'int', default: 0 }) rareCount!: number   // 稀有词数（idiom+rare）
  @Column({ type: 'int', default: 0 }) maxCombo!: number
  @Column({ type: 'smallint' }) rank!: number               // 1 胜 / 2 负（平局皆 1）
  @Column({ type: 'varchar', length: 16 }) sid!: string     // 对应 match_session
}
```

注册 `app.module.ts` entities 数组（`synchronize: true` 自动建表）。

---

## 4. 匹配服务

### 4.1 Redis 队列

- 入队：`POST /match/queue`（JWT）→ 校验不在其他队列/房间 → `LPUSH match_queue:{rankTier} userId` + `HSET match_queue_meta:{userId} {tier, enqueuedAt}` → 触发一次配对扫描
- 轮询：`GET /match/queue` → 返回 `queued`（含已等待秒）/ `matched`（含 matchId + 对局信息）/ `timeout`（等待 >30s，出队）
- 取消：`DELETE /match/queue` → 出队 + 删 meta

### 4.2 配对扫描

`MatchService` 启动 `@Interval(1000)` 扫描所有 tier 队列（tier 1-7）：

1. 每队列成对 `RPOP` 两个 userId（或从头取 2 个），配成一对
2. 取不出 2 人则保留（同 tier 优先；不足时跨 tier ±1 合并扫描）
3. 扫描中清理超时（enqueuedAt > 30s）的等待者 → 状态置 timeout，由客户端轮询消费

### 4.3 配对成功（`setupMatch(tier, userA, userB)`）

1. `gridPoolService.acquire('standard')` 取一张网格（对战固定 standard 5×5，决策 #1）
2. 建 `MatchEntity`（status ongoing、gridSeed=gridEntity.id、grid、targetWords）
3. 为 A/B 各建 `match_session`（复用 `GameService.getGrid` 返回的 sid），再 `HSET match_session:{sid} matchId`（决策 #2）
4. 建房间 `rooms.set(matchId, MatchRoom)`（status: countdown，players 两方 userId + connected 标记）
5. 向双方广播 `match_start`（含 grid/size/duration=180/对手昵称+段位/己方 sid）
6. 3s 后广播 `match_countdown`（3-2-1）→ 状态切 playing → 启动主计时

```typescript
interface MatchRoom {
  matchId: string
  difficulty: string
  grid: string[][]
  size: number
  duration: number            // 180
  players: Map<number, {    // key: userId
    sid: string
    clientConnected: boolean
    disconnectTimer?: NodeJS.Timeout
  }>
  status: 'countdown' | 'playing' | 'finished'
  remainingSec: number
  timer?: NodeJS.Timeout       // 每秒 tick
}
```

---

## 5. 对局生命周期

### 5.1 服务端权威计时

- `MatchService` 每秒 `tick(matchId)`：`remainingSec--` → 广播 `match_tick {remainingSec, scores}`（双方 score/combo 从各自 `match_session` 读，pipeline 批量）→ 归零时 `finishMatch`
- 玩家分数变更时（submit_word 后）立即额外广播一次（增量感知 <1s）

### 5.2 提词广播（GameGateway.submit_word 扩展）

```typescript
// handleSubmitWord 内，submitWord 成功后：
const session = await redis.hgetall(`match_session:${sid}`)
if (session?.matchId) {
  this.matchService.broadcastScore(session.matchId)  // 读双方 → 广播 match_tick + 对手得分飘字
}
```

- 广播内容**只含分数/连击增量，不含词**（GDD §4.2.2 防抄，决策 #4）

### 5.3 胜负判定（GDD §4.2.2）

```typescript
function decideWinner(a, b): 1 | 2 | 0 {   // 1=A胜 2=B胜 0=平
  if (a.score !== b.score) return a.score > b.score ? 1 : 2
  if (a.rareCount !== b.rareCount) return a.rareCount > b.rareCount ? 1 : 2
  if (a.maxCombo !== b.maxCombo) return a.maxCombo > b.maxCombo ? 1 : 2
  return 0
}
// rareCount = endGame 结果 foundWords 中 rarity ∈ {idiom, rare} 的词数
```

### 5.4 结算落库（`finishMatch(matchId)`）

1. 双方分别 `gameService.endGame(sid)`（图鉴批量收集 + found/unfound 数据）
2. 计算 rareCount/maxCombo → `decideWinner`
3. 更新 `MatchEntity`（status=finished、winnerId、endedAt）
4. 写 `match_players` 两行（rank 1/2；平局皆 rank 1，winnerId=null）
5. 广播 `match_end {matchId, winnerUserId, forfeit, forfeitReason, opponentForfeit, my:{...}, opponent:{...}}`（双方数据各自包装，含 foundWords 前几高亮供结算页展示；`forfeit/forfeitReason('abandon'|'disconnect')/opponentForfeit` 用于"对方认输"提示；有 `forfeitUserId` 时该方强制判负、胜负与得分无关，即 effectiveWinner 覆盖按分结果，详见 §14.5/§14.6）
6. 清 `match_session`（TTL 自然过期即可）、删房间、清 queue meta

---

## 6. 断线重连

- `GameGateway.handleDisconnect`：先经 `MatchService.isCurrentClient(userId, client)` 校验**该连接仍是用户当前连接**（旧 socket close 晚到时忽略，避免误删新连接/误启断线判负，见 §14.5.5）→ 若在房间中则 `players.get(userId).clientConnected = false` + 启动 30s 定时器；超时 → 该方判负（`finishMatch(..., 'disconnect')`，胜者为对方，与得分无关）
- 客户端重连/刷新后重入：WS 指数退避重连（迭代 5 已有），或从 `queueStatus` matched 补齐开局数据后，**补发 `match_join {matchId}`**（两者互补覆盖 WS 先/后连时序）→ 清除 30s 断线宽限定时器，避免误判负
- `GameGateway.match_join`：校验 JWT 已通过 → 查房间 → 恢复 `clientConnected = true` + 取消定时器 + 更新连接映射 → 立即回发 `match_restore {remainingSec, myScore, opponentScore, grid, size, opponent}`（若对局已结束则回 `match_end`）
- **断线期间提词不补发**（客户端队列丢弃，30s 窗口内影响可接受，技术债登记）
- **主动离开 ≠ 断线**：玩家显式退出走 HTTP `POST /match/abandon`（立即判负、对方获胜），不等待 30s（§14.5.2）

---

## 7. 消息协议（JSON，在迭代 5 基础上新增）

**客户端 → 服务端**：
```json
{ "event": "match_join", "data": { "matchId": "uuid" } }
```

**服务端 → 客户端**：
```json
{ "event": "match_start", "data": { "matchId": "uuid", "grid": [["字",...]], "size": 5, "duration": 180, "mySid": "uuid", "opponent": { "nickname": "玩家xxx", "rankTier": 1 } } }
{ "event": "match_countdown", "data": { "count": 3 } }
{ "event": "match_tick", "data": { "remainingSec": 120, "myScore": 45, "opponentScore": 60, "myCombo": 3, "opponentCombo": 1 } }
{ "event": "match_opponent_score", "data": { "delta": 5, "total": 60 } }
{ "event": "match_restore", "data": { "remainingSec": 100, "myScore": 30, "opponentScore": 20, "grid": [...], "size": 5, "opponent": {...}, "mySid": "uuid" } }
{ "event": "match_end", "data": { "matchId": "uuid", "winnerUserId": 1, "forfeit": true, "forfeitReason": "abandon|disconnect|null", "opponentForfeit": true, "my": { "score": 120, "rareCount": 3, "maxCombo": 8, "foundWords": [...] }, "opponent": { "score": 100, "rareCount": 2, "maxCombo": 5, "foundWords": [...] } } }
```

**HTTP API（JWT）**：
| API | 说明 |
|---|---|
| `POST /match/queue` | 入队（默认 standard；已在对局中则直接返回现有 `matched`，保证一人一场） |
| `GET /match/queue` | 轮询：queued / matched / timeout |
| `DELETE /match/queue` | 取消排队 |
| `POST /match/abandon` | 主动离开：立即判负，对方无论得分直接获胜（§14.5） |

---

## 8. 前端设计

### 8.1 大厅首页（新增 `client/src/pages/Home/index.vue`，GDD §6.1 P0）

- 入口聚合：**单人闯关**（→ Chapters）、**实时对战**（→ Battle）、**图鉴**（→ Pokedex）、**退出登录**（→ Login）
- `pages.json` 注册 `pages/Home/index`；`Login` 登录成功与已登录跳转改为 `reLaunch /pages/Home/index`（决策 #9）
- Chapters 页保持现状（header 图鉴/退出保留，不重复改）

### 8.2 对战页（新增 `client/src/pages/Battle/index.vue`）

三阶段状态机（`phase: 'queuing' | 'countdown' | 'playing' | 'finished'`）：

1. **queuing**：匹配动画 + 取消按钮；`setInterval` 1s 轮询 `GET /match/queue`；收到 `matched` → 切 countdown；收到 `timeout` → 提示"未匹配到对手，请重试"；WS 收到 `match_start` 同样触发（双通道兜底）
2. **countdown**：3-2-1 大字（`match_countdown`）
3. **playing**：复用 `GridBoard`（同单人），顶部双分数条（我/对手）+ 倒计时 + 对手得分飘字（`match_opponent_score`）+ 断线提示条"连接断开，重连中..."（WS onClose 置状态，onOpen/`match_restore` 清除）
4. **finished**：结算视图（复用 Result 页视觉风格：胜负文案、双方分数、稀有词数、最高连击、我方 foundWords 列表）；按钮"返回大厅"

### 8.3 store 扩展（`client/src/store/game.ts`）

新增状态：`matchMode`、`matchId`、`opponent`（nickname/rankTier/score）、`mySid`、`matchEnd`（胜负+双方数据）、`matchPhase`

- `submitSelection` 不变（统一 `submit_word`，sid 为对战返回的 `mySid`）
- `handleWsMessage` 增加 `match_start/match_countdown/match_tick/match_opponent_score/match_restore/match_end` 分发
- 对战结束 `match_end` → 存 `matchEnd` → 页面切 finished
- `restart/abandon` 清理对战状态；离开 Battle 页时 `DELETE /match/queue`（若在排队）

### 8.4 socket.ts

无改动（`connectSocket/sendWs` 通用；`match_join` 用 `sendWs` 发送）。

---

## 9. 实现顺序建议

### 阶段一：后端数据模型 + 匹配
1. `MatchEntity` + `MatchPlayerEntity` + app.module 注册（synchronize 自动建表）
2. `MatchModule`（双向 forwardRef）+ `MatchController`（queue 入队/轮询/取消）+ `MatchService`（队列、meta、配对扫描、超时）
3. `GameService.getGrid` 增加可选 `matchId` 参数（写入会话 hash）；`submitWord` 对 `session.matchId` 跳过 perfect

### 阶段二：房间 + 对局生命周期
4. `GameGateway`：`userIdToClient` Map + `broadcastToUser`；`match_join` 事件；`handleDisconnect` 房间断线标记
5. `MatchService`：`setupMatch`（取网格/建会话/建房间/match_start）、`tick` 计时、`broadcastScore`、`finishMatch`（endGame 双方 + 胜负 + 落库 + match_end）
6. `submit_word` 结果后触发 `broadcastScore`

### 阶段三：断线重连
7. 30s 断线定时器 + `match_join` 恢复 + `match_restore` 补发

### 阶段四：前端
8. `Home` 大厅页 + `pages.json` + Login 跳转改造
9. `Battle` 页（排队轮询/倒计时/对局/结算）
10. store 对战状态与 WS 分发扩展

### 阶段五：测试与验收
11. 后端单测：配对、胜负 tie-break、断线恢复、超时判负
12. H5 双标签页联调 + `build:mp-weixin` 编译通过

---

## 10. 验收标准映射

| 验收项（迭代计划） | 实现落点 |
|---|---|
| 两个真机/模拟器能匹配成功并对战一局 | §4 匹配 + §5 生命周期（H5 双标签页验证） |
| 双方网格完全相同 | §4.3 同一 gridEntity / grid_seed |
| 一方找词，另一方分数条 1 秒内更新 | §5.2 submit_word 后即时 broadcastScore |
| 一方断线 30 秒内重连能恢复 | §6 重连 + match_restore |
| 结算判胜负正确（平局看稀有词数→连击峰值） | §5.3 decideWinner |
| 匹配超时正确提示未匹配（AI 兜底为技术债 #9） | §4.1 timeout 轮询返回 |
| 微信小程序真机验证通过 | 本迭代 H5 验证，`build:mp-weixin` 编译通过；真机由开发者在微信开发者工具机器上验证（已决策 #1） |

---

## 11. 技术债

| # | 描述 | 偿还迭代 |
|---|---|---|
| 9 | AI 对手未实现（匹配兜底 + 4 人混战补位） | 8（保持待偿） |
| 10 | 对战断线期间提词丢弃不补发（30s 窗口内体验可接受） | 8（对战缓存补发） |
| - | ws 房间管理需自行实现（迭代 5 登记） | **本迭代已偿** |

---

## 12. 已决策事项（v1 定稿）

| # | 问题 | 决策 |
|---|---|---|
| 1 | 验证端 | 本迭代 H5 双标签页验证；小程序仅保证编译通过，真机验证在微信开发者工具机器上做（用户已确认） |
| 2 | 对战入口 | 新建大厅首页（GDD §6.1 P0），Login 改跳 Home（用户已确认） |
| 3 | 匹配超时 | 30s 提示未匹配，AI 兜底留迭代 8 技术债 #9（用户已确认） |
| 4 | 对战网格与时长 | 固定 standard 5×5，180 秒（GDD §4.2.1 实时对战 3 min） |
| 5 | 计分通道 | 每玩家独立 match_session 复用 submitWord/endGame 全链路，match 关联 sid；perfect 提前结束仅限单人 |
| 6 | 广播内容 | 只含分数/连击增量与"对手得分"飘字，不含词（GDD §4.2.2 防抄）；迭代计划中"对手找到词提示"降级为得分飘字 |
| 7 | 匹配键 | users.rankTier（1-7），同 tier 优先、±1 宽松；无段位概念依赖 |
| 8 | 匹配状态通道 | HTTP 轮询（1s）+ WS match_start 广播双通道，客户端以 WS 为主、轮询兜底 |
| 9 | 模块依赖 | GameModule ⇄ MatchModule 双向 forwardRef（GameGateway 广播需 MatchService，MatchService 建会话需 GameService） |
| 10 | 断线重连 | 30s 内 match_join 恢复 + match_restore 补发；超时判负（AI 接管留 8）；断线期间提词不补发（技术债 #10） |
---

## 13. 实现补充（联调发现，v1.1 增补）

| # | 问题 | 决策/修复 |
|---|---|---|
| 1 | bigint 列 userId 经 JWT 为字符串，连接注册 Map key 为 `"3"` 而广播用数字 `3` → 广播失配 | GameGateway 连接时 `Number(payload.userId)` 统一数字 |
| 2 | GameModule ⇄ MatchModule 双向 forwardRef 下 gateway 实例归属不确定，连接注册与广播可能落到不同对象 | **广播与连接映射收归 MatchService 实例**（`userClients` + `registerClient/unregisterClient/broadcastToUser`），GameGateway 只做转发，不再自行持有映射 |
| 3 | 断线 30s 超时仅触发 finishMatch 按分数结算，断线方可能因分高反而胜 | `finishMatch(matchId, forfeitUserId?)`：断线方强制判负（胜者为对方，对齐 GDD §4.2.2） |
| 4 | 重复入队（页面残留排队状态）导致自己配对自己 | `queue()` 入队前先 `cancelQueue(userId)` 清理旧排队残留 |
| 5 | 对战页 GridBoard 被 flex 布局拉高（非正方形），hitTest 行索引越界无法提词 | Battle 页 GridBoard 外包 `aspect-ratio: 1/1` 正方形容器 |
| 6 | 对战模式未设置 store.phase，GridBoard 事件被单人 phase 检查拦截 | `applyMatchStartData/match_tick/match_restore` 设置 `phase='playing'`；`match_end` 置 `phase='idle'` |
| 7 | 退出登录/换号后旧 WS 连接残留（新用户广播丢失） | 所有退出登录处 `disconnectSocket()`；Login 登录前断开旧连接；App.vue `onLaunch` 全局建连（刷新/直达页面也保持连接） |
| 8 | WS 晚连/广播丢失时开局数据缺失 | `queueStatus` matched 返回完整对局数据（grid/mySid/opponent），前端轮询兜底应用 |

---

## 14. 回归修复记录（迭代 6 真机/联调验证后 v1.2 增补）

> 以下均为联调回归（H5 双标签页 + 真实 PG/Redis/Nest/WS 环境）发现并修复的 Bug，按根因归类。改动同时涉及前几次网格交互（14.1）与对战全链路（14.2-14.6）。

### 14.1 点击方格上半部分被识别为上方格（uni-app H5 坐标系混用）

- **现象**：对局中点击格子**上半部分**（甚至中心）被选中为**上方格**；第一行上半部分点击无反应。
- **根因**：uni-app H5 运行时会对事件对象做**坐标系换算**——`normalizeMouseEvent`/`normalizeTouchEvent` 会把 `clientY` 统一换算到"页面显示区"坐标系：减去 `getWindowTop()`（= CSS 变量 `--window-top` + safe-area-top，默认导航栏时即导航栏高度 44px）。而 `GridBoard` 的实时校准 `liveBoardRect` 用 `getBoundingClientRect()` 返回的是**浏览器视口坐标**。两种坐标系混用 → 命中矩形整体下移一个顶部窗口高度（≈ 一格的 71%），点上半格被算成上方格；`clientX` 不受影响，所以只有纵向错位。
- **修复**（`client/src/components/GridBoard.vue`）：
  - `getWindowTopOffset()`：按 uni-h5 同口径读取 `--window-top`（取第一个数字），>0 才生效；
  - `readLiveRect()`：`getBoundingClientRect()` 后把 `top` 减去该偏移，使命中矩形与事件坐标**同空间**；`left/x` 不偏移（与 uni 行为一致）；
  - `refreshBoardPos/refreshLivePos` 统一走 `readLiveRect()`，滚动/resize 后仍准确。
- **关联**：`core/gridHit.ts` 行按 `height/size`、列按 `width/size` 等分（纯函数，vitest 10 例，含非正方形断言）。
- **验证**：headless Firefox 逐格探测上部 6%/25%/50% 点击 75/75 命中正确；滚动 120px 后仍正确；dev 与生产构建均通过。

### 14.2 对战一方实时得分始终为 0（broadcastScores pipeline 漏读）

- **现象**：一方连词后**自己/对手的实时分数一直是 0**，但结算页有分。
- **根因**：`MatchService.broadcastScores` 的 Redis pipeline **只压入一条** `hgetall(match_session:{pa.sid})`，下面却读 `results[1]` 取 pb 的分数 → `results[1]` 为 `undefined` → `scoreB` 回退 `room.lastScores` 初值 0 且永不更新（comboB 同理）。广播给双方后：房间第二个玩家看到的自己分数、以及双方看到的对手分数全是 0。
- **修复**（`server/src/match/match.service.ts`）：补第二条 `pipe.hgetall(match_session:{pb.sid})`。
- **验证**：E2E 双方实时分数条随提词同步更新。

### 14.3 对战网格 / 找到词语列表居中与单人闯关不一致

- **现象**：H5 全屏时对战网格偏左；找到的词语列表显示在左侧。
- **根因**：`.board-wrap { width:100%; aspect-ratio:1/1 }` 撑满容器但内部 `GridBoard` 的 620rpx 固定宽**靠左**排；`.found-words { width:100% }` 同理，词条在容器内左对齐。
- **修复**（`client/src/pages/Battle/index.vue`）：
  - `.board-wrap`：620rpx 定宽 + `align-self:center` + `justify-content/align-items:center`；
  - `.found-words`：620rpx + `align-self:center`（与单人闯关 `align-items:center` 等效居中）。
- **验证**：按编译后 CSS 在 1366px 视口实测，棋盘与词语列表 center 一致（与视口中心仅差页面 padding）。

### 14.4 对战缺少错词红闪与连击额外加分反馈

- **现象**：对战页无错词背景红闪、无连击条/额外加分显示，与单人闯关不一致。
- **根因**：失败反馈（`failFlash`）与连击条（10s 倒计时 + 额外加分）只在单人 `Game` 页实现；`Battle` 页只显示找词计数。服务端 `submitWord` 对任意会话（含对战）都计算连击/`comboBonus`，是纯前端缺失展示。
- **修复**（`client/src/pages/Battle/index.vue`，复刻 Game 页）：
  - 错词：watch `store.lastFeedback==='fail'` → 根节点 `battle-fail` 背景红闪 + `playFail()` + `uni.vibrateShort`，350ms 复原；
  - 连击：`startComboCountdown()`（10s 进度条）+ `连击 ×N（本次 +X分）` 文案，成功且 `combo>=1` 时 `playCombo()`；
  - 定时器随 `onUnmounted` 清理。
- **验证**：构建产物含 `.battle-fail`/`.combo-bar` 与连击递减逻辑。

### 14.5 双方刷新页面后重复匹配出两场对战（对局生命周期核心 Bug）

- **现象**：一场对战未结束时双方刷新页面 → 重新匹配并配在一起，**产生两场对局**，两场的倒计时/得分分别闪烁。
- **根因链**：
  1. **userId 类型不一致（根本原因）**：`UserEntity.id` 为 `@PrimaryGeneratedColumn({ type: 'bigint' })`，node-postgres 以**字符串**返回 → JWT 的 `userId` 是字符串。WS 网关已 `Number(payload.userId)`（房间/`playerMatch` 用数字 key），但 **HTTP 的 `JwtAuthGuard` 直传字符串** → `MatchController` 里 `playerMatch.get("11")` 永远 miss → 服务端误判"用户不在对局中" → 重新入队配出新房间，旧房间仍广播 → **双房间、双广播**（倒计时/分数闪烁）。
  2. 主动离开（返回/退出登录）只清前端状态，服务端对局未结束 → 残留。
  3. 重连进入时未 `match_join` 重新声明在线 → 30s 断线宽限定时器未清除 → 误判负。
  4. 旧 socket 的 `close` 若晚于新连接到达，`unregisterClient` 会误删**新**连接映射。
- **修复**：
  1. `server/src/auth/jwt.guard.ts`：`req.user.userId = Number(payload.userId)`（与 WS 统一，一键修复 HTTP 侧全部 userId Map 判定）。
  2. **主动离开**：新增 `POST /api/match/abandon` + `MatchService.abandon()`——离场者立即判负、**对方无论得分高低直接获胜**，同时清玩家映射与 Redis 排队残留。
  3. `finishMatch(matchId, forfeitUserId?, forfeitReason?)` 引入 **effectiveWinner**（判负覆盖按分结果），`won/rank/winnerUserId` 三者统一，不再出现"实际赢了却显示惜败"。
  4. **断网重进续对局**：重进时轮询 `queueStatus` 命中 `matched` 应用开局数据后补发 WS `match_join`（清除 30s 宽限定时器 + 收 `match_restore`）；与 `connectWs` 重连回调（`matchMode` 下自动 `match_join`）互补，覆盖 WS 先连/后连两种时序。服务端 `queue()` 对已在对局中的人本就返回现有 `matchId`，配合该修复即满足"一个玩家同一时间只有一场对局"。
  5. **连接身份校验**：`MatchService.isCurrentClient(userId, client)`，`unregisterClient` 仅当仍是当前连接时才清理；`GameGateway.handleDisconnect` 旧连接 close 晚到时记 `stale close ignored`，不再误删新连接/误启断线判负。
- **验证**（真实 PG+Redis+Nest+WS E2E）：
  - 双方刷新+重连+重新入队 → 均返回**同一 matchId**（不再产生第二场）；`match_join` 后双方收同一场 `match_restore`；
  - A 主动认输 → B 收到 `match_end{winnerUserId=B, won:true, opponentForfeit:true}`（与得分无关）；A 再入队不再是 `matched`（无残留）。

### 14.6 结算页对方认输无提示，低分获胜易误解

- **现象**：对方认输（主动离开/断线）后，胜利方若分数更低，结算页只显示"胜利！"，用户会误解。
- **修复**：
  - 服务端 `match_end` 广播增加：`forfeit`（是否判负）、`forfeitReason`（`'abandon' | 'disconnect' | null`）、`opponentForfeit`（**按双方视角**计算，己方是否因对方判负而胜）；`finishMatch` 增加 `forfeitReason` 参数，`abandon`→`'abandon'`、断线 30s 超时→`'disconnect'`、正常计完→`null`。
  - 前端（`client/src/pages/Battle/index.vue` 结算区）：`opponentForfeit` 时在标题下显示提示条 **「对方认输，本局胜利」/「对方掉线，本局判胜」**，并在对手昵称右侧加红色徽标 **「认输」/「掉线」**；正常结束不显示。
- **验证**：E2E 确认 A 认输后 B 收到的 `match_end` 含 `forfeit:true, forfeitReason:'abandon', opponentForfeit:true`；构建产物含提示文案与样式。

> 协议/接口变更汇总：`match_end` 新增 `forfeit/forfeitReason/opponentForfeit` 字段（§7）；新增 HTTP `POST /match/abandon`（§7）；`queueStatus` matched 重入场景客户端需补发 `match_join`（§6）。
